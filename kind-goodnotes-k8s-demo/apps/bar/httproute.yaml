apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: bar
  namespace: apps
spec:
  parentRefs:
    - name: public-gateway
      namespace: gateway
  hostnames:
    - bar.localhost
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: bar
          port: 80
