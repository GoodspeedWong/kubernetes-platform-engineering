apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: foo
  namespace: apps
spec:
  parentRefs:
    - name: public-gateway
      namespace: gateway
  hostnames:
    - foo.localhost
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: foo
          port: 80
